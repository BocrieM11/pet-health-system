# 🌐 宠物健康系统 - 在线部署完整指南

> 从本地项目到在线Web应用，让用户通过网址直接访问
> 
> 目标：部署后用户访问 `https://你的域名.com` 即可使用
> 
> 撰写日期：2026-04-19

---

## 📋 目录

1. [部署概览](#部署概览)
2. [方案一：免费部署（推荐学习/演示）](#方案一免费部署推荐学习演示)
3. [方案二：低成本云服务器（推荐小规模商用）](#方案二低成本云服务器推荐小规模商用)
4. [方案三：企业级部署（大规模商用）](#方案三企业级部署大规模商用)
5. [域名与HTTPS配置](#域名与https配置)
6. [自动部署CI/CD](#自动部署cicd)

---

## 🎯 部署概览

### 什么是在线部署？

**本地运行 vs 在线部署**

```
❌ 本地运行（当前）
用户 → 下载代码 → 安装Node.js → npm install → npm run dev → http://localhost:3000

✅ 在线部署（目标）
用户 → 浏览器输入网址 → https://pet-health.com → 直接使用
```

### 需要部署的内容

你的系统需要部署**三个部分**：

```
┌─────────────────────────────────────────┐
│  1. 前端（React应用）                     │
│     需要：静态文件托管服务                 │
│     例如：Vercel, Netlify, Nginx         │
└─────────────────────────────────────────┘
              ↓ API请求
┌─────────────────────────────────────────┐
│  2. 后端（Node.js + Express）            │
│     需要：Node.js运行环境 + 持续运行       │
│     例如：云服务器, Railway, Render       │
└─────────────────────────────────────────┘
              ↓ 数据读写
┌─────────────────────────────────────────┐
│  3. 数据库（SQLite/PostgreSQL）          │
│     需要：持久化存储 + 自动备份            │
│     例如：云数据库, PlanetScale, Supabase │
└─────────────────────────────────────────┘
```

---

## 🆓 方案一：免费部署（推荐学习/演示）

**适用场景：** 学习项目、毕业设计演示、个人使用  
**成本：** 完全免费  
**限制：** 并发有限（约100用户），免费域名或自定义域名

### 架构方案

```
前端：Vercel（免费）
后端：Railway 或 Render（免费额度）
数据库：Railway内置SQLite 或 迁移到PostgreSQL
```

---

### 步骤1：部署前端到Vercel

#### 1.1 准备前端代码

**修改API地址配置**

```javascript
// frontend/src/services/api.js
import axios from 'axios';

// 根据环境自动切换API地址
const API_BASE_URL = import.meta.env.VITE_API_URL || 
  (import.meta.env.MODE === 'production' 
    ? 'https://你的后端域名.railway.app/api'  // 生产环境
    : '/api');  // 开发环境（Vite代理）

const api = axios.create({
  baseURL: API_BASE_URL,
  timeout: 10000
});

// ... 其他代码保持不变
export default api;
```

**创建环境变量文件**

```env
# frontend/.env.production
VITE_API_URL=https://pet-health-api.railway.app/api
```

**配置build脚本**

```json
// frontend/package.json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

#### 1.2 部署到Vercel

**方法一：通过GitHub（推荐）**

1. **创建GitHub仓库**
```bash
# 在项目根目录
cd C:\Users\Lenovo\Desktop\pet-health-system

# 初始化Git（如果还没有）
git init
git add .
git commit -m "准备部署"

# 关联GitHub仓库
git remote add origin https://github.com/你的用户名/pet-health-system.git
git branch -M main
git push -u origin main
```

2. **连接Vercel**
- 访问 https://vercel.com
- 使用GitHub账号登录
- 点击 "New Project"
- 选择你的仓库 `pet-health-system`
- **重要配置：**
  ```
  Framework Preset: Vite
  Root Directory: frontend
  Build Command: npm run build
  Output Directory: dist
  ```
- 添加环境变量：
  ```
  VITE_API_URL = https://你的后端域名.railway.app/api
  ```
- 点击 "Deploy"

3. **部署完成**
- 等待2-3分钟
- 获得URL：`https://pet-health-系统随机字符.vercel.app`
- 可以自定义域名（后面讲）

**方法二：命令行部署**

```bash
# 安装Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
cd frontend
vercel

# 按提示操作：
# - Set up and deploy? Y
# - Which scope? 选择你的账号
# - Link to existing project? N
# - Project name? pet-health-frontend
# - Directory? ./ (当前目录)
# - Override settings? N

# 生产部署
vercel --prod
```

---

### 步骤2：部署后端到Railway

#### 2.1 准备后端代码

**修改数据库配置（支持PostgreSQL）**

```javascript
// backend/database.js
const { Pool } = require('pg');
const sqlite3 = require('sqlite3').verbose();
const path = require('path');

let db;

// 根据环境选择数据库
if (process.env.DATABASE_URL) {
  // 生产环境：使用PostgreSQL
  console.log('✅ 使用PostgreSQL数据库');
  
  const pool = new Pool({
    connectionString: process.env.DATABASE_URL,
    ssl: {
      rejectUnauthorized: false
    }
  });

  // 封装统一的数据库接口
  db = {
    all: (sql, params) => pool.query(sql, params).then(res => res.rows),
    get: (sql, params) => pool.query(sql, params).then(res => res.rows[0]),
    run: (sql, params) => pool.query(sql, params)
  };

  // 初始化PostgreSQL表结构
  initPostgresDatabase(pool);

} else {
  // 开发环境：使用SQLite
  console.log('✅ 使用SQLite数据库');
  
  const dbPath = path.join(__dirname, 'pet_health.db');
  const sqliteDb = new sqlite3.Database(dbPath);

  // 封装Promise接口
  db = {
    all: (sql, params) => new Promise((resolve, reject) => {
      sqliteDb.all(sql, params || [], (err, rows) => {
        if (err) reject(err);
        else resolve(rows);
      });
    }),
    get: (sql, params) => new Promise((resolve, reject) => {
      sqliteDb.get(sql, params || [], (err, row) => {
        if (err) reject(err);
        else resolve(row);
      });
    }),
    run: (sql, params) => new Promise((resolve, reject) => {
      sqliteDb.run(sql, params || [], function(err) {
        if (err) reject(err);
        else resolve({ lastID: this.lastID, changes: this.changes });
      });
    })
  };

  initSQLiteDatabase(sqliteDb);
}

async function initPostgresDatabase(pool) {
  // 使用PostgreSQL语法创建表
  await pool.query(`
    CREATE TABLE IF NOT EXISTS users (
      id SERIAL PRIMARY KEY,
      username VARCHAR(255) UNIQUE NOT NULL,
      email VARCHAR(255) UNIQUE NOT NULL,
      password VARCHAR(255) NOT NULL,
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    )
  `);

  await pool.query(`
    CREATE TABLE IF NOT EXISTS pets (
      id SERIAL PRIMARY KEY,
      user_id INTEGER REFERENCES users(id),
      name VARCHAR(255) NOT NULL,
      species VARCHAR(100) NOT NULL,
      breed VARCHAR(100),
      gender VARCHAR(20),
      birth_date DATE,
      weight DECIMAL(10,2),
      avatar TEXT,
      owner_name VARCHAR(255),
      owner_phone VARCHAR(50),
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    )
  `);

  // ... 其他表（需要将SQLite语法转为PostgreSQL语法）

  // 创建索引
  await pool.query('CREATE INDEX IF NOT EXISTS idx_pets_user_id ON pets(user_id)');
  // ... 其他索引

  console.log('✅ PostgreSQL数据库初始化完成');
}

function initSQLiteDatabase(db) {
  // 原有的SQLite初始化代码
  // ... 保持不变
}

module.exports = db;
```

**添加依赖**

```bash
cd backend
npm install pg
```

**修改package.json**

```json
{
  "name": "pet-health-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

**允许跨域（生产环境）**

```javascript
// backend/server.js
const cors = require('cors');

// 配置CORS
const corsOptions = {
  origin: process.env.FRONTEND_URL || 'http://localhost:3000',
  credentials: true,
  optionsSuccessStatus: 200
};

app.use(cors(corsOptions));
```

**创建环境变量示例**

```env
# backend/.env.example
# JWT密钥（生产环境必须修改）
JWT_SECRET=your-super-secret-key-change-in-production

# Anthropic AI密钥
ANTHROPIC_API_KEY=your-anthropic-api-key

# 数据库连接（Railway会自动注入）
DATABASE_URL=postgresql://user:password@host:port/database

# 前端地址（CORS白名单）
FRONTEND_URL=https://pet-health.vercel.app

# 服务器端口
PORT=3001
```

#### 2.2 部署到Railway

**方法一：通过GitHub（推荐）**

1. **确保代码已推送到GitHub**
```bash
git add .
git commit -m "准备后端部署"
git push
```

2. **连接Railway**
- 访问 https://railway.app
- 使用GitHub账号登录
- 点击 "New Project"
- 选择 "Deploy from GitHub repo"
- 选择你的仓库 `pet-health-system`
- **重要配置：**
  ```
  Root Directory: backend
  Start Command: npm start
  ```

3. **添加PostgreSQL数据库**
- 在项目中点击 "+ New"
- 选择 "Database" → "PostgreSQL"
- Railway会自动创建数据库并注入 `DATABASE_URL` 环境变量

4. **配置环境变量**
- 点击后端服务 → "Variables"
- 添加环境变量：
  ```
  JWT_SECRET=你的超级密钥-至少32位随机字符
  ANTHROPIC_API_KEY=sk-ant-xxxx（如果有）
  FRONTEND_URL=https://pet-health.vercel.app
  NODE_ENV=production
  ```

5. **部署完成**
- 点击 "Deploy"
- 等待部署完成
- 获得URL：`https://pet-health-api.up.railway.app`
- 复制这个URL

6. **更新前端环境变量**
- 回到Vercel
- Settings → Environment Variables
- 修改 `VITE_API_URL` 为Railway提供的URL
- 重新部署前端

**方法二：Railway CLI**

```bash
# 安装Railway CLI
npm install -g @railway/cli

# 登录
railway login

# 初始化项目
cd backend
railway init

# 添加PostgreSQL
railway add postgresql

# 部署
railway up

# 打开仪表板
railway open
```

---

### 步骤3：配置域名（可选）

#### 3.1 注册域名

**免费域名选项：**
- Freenom（.tk, .ml, .ga等）
- EU.org（需要审核）

**付费域名（推荐）：**
- Namecheap：约$10/年
- 阿里云：约¥55/年
- 腾讯云：约¥29/年

#### 3.2 配置DNS

**在Vercel配置前端域名**

1. Vercel项目 → Settings → Domains
2. 添加域名：`www.pet-health.com`
3. 按提示添加DNS记录：
   ```
   类型：CNAME
   名称：www
   值：cname.vercel-dns.com
   ```

**在Railway配置后端域名**

1. Railway项目 → Settings → Networking
2. 添加自定义域名：`api.pet-health.com`
3. 添加DNS记录：
   ```
   类型：CNAME
   名称：api
   值：你的项目.up.railway.app
   ```

#### 3.3 启用HTTPS

**好消息：Vercel和Railway都自动提供免费SSL证书！**

- Vercel：自动使用Let's Encrypt
- Railway：自动HTTPS

部署后自动变为：
- 前端：`https://www.pet-health.com`
- 后端：`https://api.pet-health.com`

---

### 步骤4：测试部署

**测试清单：**

```bash
# 1. 测试后端健康检查
curl https://你的后端域名.railway.app/api/test

# 2. 测试用户注册
curl -X POST https://你的后端域名.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password":"123456"}'

# 3. 访问前端
打开浏览器访问: https://你的前端域名.vercel.app
```

**前端测试步骤：**
1. ✅ 打开网站能正常显示
2. ✅ 注册新用户
3. ✅ 登录成功
4. ✅ 添加宠物
5. ✅ 上传头像
6. ✅ 添加健康记录
7. ✅ 查看统计图表

---

### 免费方案成本总结

| 项目 | 服务 | 免费额度 | 限制 |
|------|------|---------|------|
| 前端托管 | Vercel | 无限 | 100GB流量/月 |
| 后端服务 | Railway | $5/月额度 | 约500小时运行时间 |
| 数据库 | Railway PostgreSQL | 1GB存储 | 共享CPU |
| 域名 | Freenom | 免费 | .tk/.ml等后缀 |
| SSL证书 | Let's Encrypt | 免费 | 自动续期 |

**总成本：$0/月** （Railway免费额度够用）

---

## 💰 方案二：低成本云服务器（推荐小规模商用）

**适用场景：** 小型商业应用、创业项目  
**成本：** ¥50-200/月  
**性能：** 支持1000+并发用户

### 架构方案

```
云服务器：阿里云/腾讯云 轻量应用服务器
配置：2核2GB内存，3Mbps带宽
操作系统：Ubuntu 22.04
部署方式：Docker Compose一键部署
```

---

### 步骤1：购买云服务器

**推荐配置：**

| 供应商 | 配置 | 价格 | 链接 |
|-------|------|------|------|
| 阿里云 | 2核2G,3M带宽 | ¥74/月 | [新用户优惠](https://www.aliyun.com) |
| 腾讯云 | 2核2G,3M带宽 | ¥62/月 | [学生优惠](https://cloud.tencent.com) |
| 华为云 | 2核2G,3M带宽 | ¥59/月 | [活动价](https://huaweicloud.com) |

**购买流程：**
1. 注册账号（实名认证）
2. 选择"轻量应用服务器"
3. 镜像选择：Ubuntu 22.04
4. 地域：选择离用户近的（如华东、华北）
5. 购买时长：建议先买1-3个月测试

---

### 步骤2：配置服务器环境

#### 2.1 连接服务器

**Windows用户：**
```bash
# 使用自带的SSH客户端
ssh root@你的服务器IP

# 或使用PuTTY、MobaXterm等工具
```

**首次登录后修改密码：**
```bash
passwd
# 输入新密码（两次）
```

#### 2.2 安装Docker

```bash
# 更新包管理器
apt update && apt upgrade -y

# 安装Docker
curl -fsSL https://get.docker.com | bash

# 启动Docker
systemctl start docker
systemctl enable docker

# 安装Docker Compose
apt install docker-compose -y

# 验证安装
docker --version
docker-compose --version
```

#### 2.3 安装Nginx

```bash
apt install nginx -y
systemctl start nginx
systemctl enable nginx
```

---

### 步骤3：部署应用

#### 3.1 上传代码到服务器

**方法一：Git克隆**
```bash
# 在服务器上
cd /var/www
git clone https://github.com/你的用户名/pet-health-system.git
cd pet-health-system
```

**方法二：FTP上传**
- 使用FileZilla等工具
- 上传整个项目目录到 `/var/www/pet-health-system`

#### 3.2 创建Docker配置

**创建docker-compose.yml**

```yaml
# /var/www/pet-health-system/docker-compose.yml
version: '3.8'

services:
  # PostgreSQL数据库
  postgres:
    image: postgres:15
    container_name: pet-health-db
    restart: always
    environment:
      POSTGRES_USER: petuser
      POSTGRES_PASSWORD: 你的超级密码
      POSTGRES_DB: pet_health
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - pet-network

  # 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pet-health-backend
    restart: always
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://petuser:你的超级密码@postgres:5432/pet_health
      JWT_SECRET: 你的超级密钥-至少32位随机字符
      FRONTEND_URL: https://你的域名.com
      PORT: 3001
    volumes:
      - ./backend/uploads:/app/uploads
    ports:
      - "3001:3001"
    depends_on:
      - postgres
    networks:
      - pet-network

  # 前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: https://你的域名.com/api
    container_name: pet-health-frontend
    restart: always
    ports:
      - "3000:80"
    networks:
      - pet-network

volumes:
  postgres_data:

networks:
  pet-network:
    driver: bridge
```

**创建后端Dockerfile**

```dockerfile
# backend/Dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY . .

# 创建uploads目录
RUN mkdir -p uploads

EXPOSE 3001

CMD ["node", "server.js"]
```

**创建前端Dockerfile**

```dockerfile
# frontend/Dockerfile
FROM node:18-alpine as build

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 构建参数
ARG VITE_API_URL
ENV VITE_API_URL=$VITE_API_URL

# 构建生产版本
RUN npm run build

# 使用Nginx服务静态文件
FROM nginx:alpine

# 复制构建产物
COPY --from=build /app/dist /usr/share/nginx/html

# 复制Nginx配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

**创建Nginx配置**

```nginx
# frontend/nginx.conf
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 3.3 启动服务

```bash
# 构建并启动所有服务
docker-compose up -d --build

# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f backend
docker-compose logs -f frontend

# 等待数据库初始化（约30秒）
# 检查后端健康状态
curl http://localhost:3001/api/test
```

---

### 步骤4：配置Nginx反向代理

**编辑主Nginx配置**

```nginx
# /etc/nginx/sites-available/pet-health
server {
    listen 80;
    server_name 你的域名.com www.你的域名.com;

    # 前端
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 后端API
    location /api/ {
        proxy_pass http://localhost:3001/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 文件上传大小限制
    client_max_body_size 10M;
}
```

**启用配置**

```bash
# 创建软链接
ln -s /etc/nginx/sites-available/pet-health /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启Nginx
systemctl restart nginx
```

---

### 步骤5：配置HTTPS（免费SSL）

**使用Let's Encrypt**

```bash
# 安装Certbot
apt install certbot python3-certbot-nginx -y

# 自动配置SSL
certbot --nginx -d 你的域名.com -d www.你的域名.com

# 按提示操作：
# - 输入邮箱
# - 同意条款（Y）
# - 是否分享邮箱（N）
# - 重定向HTTP到HTTPS（2）

# 自动续期（每天凌晨2点检查）
echo "0 2 * * * root certbot renew --quiet" >> /etc/crontab
```

**配置完成后，Nginx配置会自动更新为：**

```nginx
server {
    listen 443 ssl http2;
    server_name 你的域名.com;

    ssl_certificate /etc/letsencrypt/live/你的域名.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/你的域名.com/privkey.pem;

    # ... 其他配置
}

# HTTP自动跳转HTTPS
server {
    listen 80;
    server_name 你的域名.com;
    return 301 https://$server_name$request_uri;
}
```

---

### 步骤6：数据库备份

**创建自动备份脚本**

```bash
# /root/backup-db.sh
#!/bin/bash

BACKUP_DIR="/var/backups/postgres"
DATE=$(date +%Y%m%d_%H%M%S)
FILENAME="pet_health_backup_$DATE.sql"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
docker exec pet-health-db pg_dump -U petuser pet_health > $BACKUP_DIR/$FILENAME

# 压缩备份
gzip $BACKUP_DIR/$FILENAME

# 删除30天前的备份
find $BACKUP_DIR -name "*.gz" -mtime +30 -delete

echo "备份完成: $FILENAME.gz"
```

**添加定时任务**

```bash
# 添加执行权限
chmod +x /root/backup-db.sh

# 编辑定时任务
crontab -e

# 添加每天凌晨3点备份
0 3 * * * /root/backup-db.sh >> /var/log/backup.log 2>&1
```

---

### 步骤7：监控与维护

**安装监控工具**

```bash
# 安装htop（资源监控）
apt install htop -y

# 查看系统资源
htop

# 查看Docker容器资源使用
docker stats
```

**日志管理**

```bash
# 查看后端日志
docker-compose logs -f --tail=100 backend

# 查看Nginx日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# 日志轮转（防止日志文件过大）
apt install logrotate -y
```

**重启服务**

```bash
# 重启所有服务
docker-compose restart

# 仅重启后端
docker-compose restart backend

# 更新代码后重新部署
git pull
docker-compose up -d --build
```

---

### 低成本方案总结

| 项目 | 成本 | 说明 |
|------|------|------|
| 云服务器 | ¥60-80/月 | 2核2G配置 |
| 域名 | ¥30-60/年 | .com域名 |
| SSL证书 | 免费 | Let's Encrypt |
| 数据库 | 免费 | 自建PostgreSQL |

**总成本：约¥80/月**

**性能指标：**
- 并发用户：500-1000
- 响应时间：<200ms
- 可用性：99%+

---

## 🚀 方案三：企业级部署（大规模商用）

**适用场景：** 正式商业化产品，10,000+用户  
**成本：** ¥500-2000/月  
**性能：** 支持10,000+并发用户

### 架构方案

```
┌─────────────────────────────────────────┐
│         CDN（静态资源加速）              │
│         阿里云CDN / 腾讯云CDN            │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│           负载均衡 SLB                   │
│           流量分发到多台服务器            │
└──────────────┬──────────────────────────┘
               │
    ┌──────────┼──────────┐
    │          │          │
┌───▼───┐  ┌──▼──┐  ┌───▼───┐
│ Web1  │  │ Web2 │  │ Web3  │  ← 应用服务器集群
│ 4核8G │  │ 4核8G│  │ 4核8G │
└───┬───┘  └──┬──┘  └───┬───┘
    │         │          │
    └─────────┼──────────┘
              │
    ┌─────────▼──────────┐
    │     Redis集群       │  ← 缓存层
    │    （主从复制）      │
    └─────────┬──────────┘
              │
    ┌─────────▼──────────┐
    │  PostgreSQL主从    │  ← 数据库层
    │  （读写分离）       │
    └────────────────────┘
```

### 核心配置

**1. 负载均衡配置**
- 阿里云SLB或腾讯云CLB
- 健康检查：/api/health
- 会话保持：基于Cookie
- HTTPS卸载：在LB层终止SSL

**2. 应用服务器**
- 3台 ECS（4核8GB）
- 使用PM2集群模式
- 自动重启 + 日志管理

**3. 数据库**
- RDS PostgreSQL（主从配置）
- 8核16GB，100GB SSD
- 自动备份 + 读写分离

**4. 缓存层**
- Redis集群（2主2从）
- 4GB内存
- 持久化 + 哨兵模式

**5. 对象存储**
- 阿里云OSS / 腾讯云COS
- 存储用户上传的图片
- CDN加速访问

**6. 监控告警**
- 云监控服务
- 短信/邮件告警
- 实时性能大盘

### 成本预估

| 项目 | 配置 | 价格 |
|------|------|------|
| 应用服务器×3 | 4核8G | ¥600/月 |
| RDS数据库 | 8核16G | ¥800/月 |
| Redis | 4GB | ¥200/月 |
| 负载均衡 | 标准型 | ¥100/月 |
| OSS存储 | 100GB | ¥20/月 |
| CDN流量 | 500GB | ¥100/月 |

**总成本：约¥1,820/月**

---

## 🔧 自动部署CI/CD

### 使用GitHub Actions自动部署

**创建部署工作流**

```yaml
# .github/workflows/deploy.yml
name: Deploy to Production

on:
  push:
    branches:
      - main  # 推送到main分支时触发

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout代码
        uses: actions/checkout@v3

      - name: 设置Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 安装前端依赖
        run: |
          cd frontend
          npm ci

      - name: 构建前端
        env:
          VITE_API_URL: ${{ secrets.API_URL }}
        run: |
          cd frontend
          npm run build

      - name: 部署到服务器
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "frontend/dist/*"
          target: "/var/www/pet-health"

      - name: 重启后端服务
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/pet-health-system
            git pull
            docker-compose restart backend

      - name: 发送通知
        if: always()
        run: |
          echo "部署完成！"
```

**配置GitHub Secrets**

在GitHub仓库设置中添加：
- `API_URL`: 后端API地址
- `SERVER_HOST`: 服务器IP
- `SERVER_USER`: SSH用户名
- `SSH_PRIVATE_KEY`: SSH私钥

**效果：**
- 推送代码到GitHub → 自动构建 → 自动部署 → 自动重启
- 全程无需手动操作

---

## 📝 最终部署清单

### 方案对比

| 对比项 | 免费部署 | 低成本服务器 | 企业级 |
|-------|---------|------------|--------|
| 成本 | ¥0 | ¥80/月 | ¥1800/月 |
| 并发用户 | 100 | 1000 | 10,000+ |
| 性能 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 可用性 | 95% | 99% | 99.9% |
| 部署难度 | 简单 | 中等 | 复杂 |
| 适用场景 | 学习/演示 | 小型商用 | 大型商用 |

### 推荐选择

**如果是：**
- ✅ 毕业设计/学习项目 → **免费部署（Vercel + Railway）**
- ✅ 创业MVP/小型产品 → **低成本服务器（阿里云/腾讯云）**
- ✅ 正式商业化产品 → **企业级部署**

---

## 🎯 下一步行动

### 立即开始部署（推荐免费方案）

**30分钟快速部署清单：**

```bash
# ✅ 第1步：注册账号（5分钟）
- Vercel账号（https://vercel.com）
- Railway账号（https://railway.app）
- 使用GitHub账号登录

# ✅ 第2步：推送代码到GitHub（5分钟）
cd C:\Users\Lenovo\Desktop\pet-health-system
git init
git add .
git commit -m "准备部署"
git remote add origin https://github.com/你的用户名/pet-health-system.git
git push -u origin main

# ✅ 第3步：部署后端到Railway（10分钟）
- 连接GitHub仓库
- 添加PostgreSQL数据库
- 配置环境变量
- 等待部署完成
- 复制后端URL

# ✅ 第4步：部署前端到Vercel（10分钟）
- 连接GitHub仓库
- 配置Root Directory: frontend
- 添加环境变量 VITE_API_URL
- 等待部署完成
- 获取前端URL

# ✅ 第5步：测试（5分钟）
- 访问前端URL
- 注册账号
- 添加宠物
- 验证功能正常
```

---

## ❓ 常见问题

**Q1: 部署后打开网站一片空白？**
A: 检查浏览器控制台，通常是API地址配置错误，确保 `VITE_API_URL` 正确。

**Q2: 后端API调用失败？**
A: 检查CORS配置，确保后端允许前端域名访问。

**Q3: 数据库连接失败？**
A: Railway会自动注入 `DATABASE_URL`，检查环境变量是否正确。

**Q4: 图片上传失败？**
A: 检查后端 `uploads` 目录权限，或考虑迁移到OSS对象存储。

**Q5: 部署后性能很慢？**
A: 免费服务器资源有限，考虑升级到付费方案或添加Redis缓存。

---

**需要我帮你完成部署吗？我可以：**
1. 生成所有配置文件
2. 修改代码适配部署环境
3. 指导每一步操作
4. 解决部署中遇到的问题

告诉我你想用哪个方案，我立即开始帮你！🚀
